general:
  enable_incomplete_verification: False
model:
  onnx_quirks: "{'Reshape': {'fix_batch_size': True}}"
solver:
  bound_prop_method: crown
  crown:
    batch_size: 32
bab:
  branching:
    input_split:
      enable: True
attack:
  pgd_order: before
